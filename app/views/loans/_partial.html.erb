<%	if @loan.errors.any?  %>
    <div id="error_explanation">
      <div class="alert alert-danger" role="alert">
        <strong>Oh snap!</strong> <%= pluralize(@loan.errors.count, "error") %> have prevented the loan from being recorded.
      </div>
      <ul>
        <% @loan.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<%	end  %>